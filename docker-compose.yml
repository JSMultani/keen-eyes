services:
  keen-eyes:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: sh -c "pip install -e . && python -m unittest discover -s tests && keen-eyes run --task tasks/sample-feature.md --project examples/secure_document_workflow --out runs/container"

