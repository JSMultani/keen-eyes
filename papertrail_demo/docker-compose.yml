services:
  papertrail-demo:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "pip install -r requirements.txt && python -m app.database --reset && uvicorn app.main:app --host 127.0.0.1 --port 8001"
    ports:
      - "127.0.0.1:8001:8001"

